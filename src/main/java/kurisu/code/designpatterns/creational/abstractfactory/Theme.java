package kurisu.code.designpatterns.creational.abstractfactory;

/**
 * DEPRECATED
 * no longer need it since we're sending the factory instead of enum
 * to render the appropiate theme.
 */
public enum Theme {
    MATERIAL,
    ANT;
}
