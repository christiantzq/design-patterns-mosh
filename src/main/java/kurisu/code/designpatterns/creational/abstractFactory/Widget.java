package kurisu.code.designpatterns.creational.abstractFactory;

/**
 * Nice to have wrapper for the render() method.
 * 
 * Which makes sense in this case, but adds to the boilerplate code.
 */
public interface Widget {
    void render();
}
