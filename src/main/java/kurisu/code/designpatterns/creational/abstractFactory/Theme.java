package kurisu.code.designpatterns.creational.abstractFactory;

/**
 * DEPRECATED
 * 
 * No need for it since we're sending the factory as parameter
 * to render the appropriate theme.
 */
@Deprecated
public enum Theme {
    MATERIAL,
    ANT;
}
